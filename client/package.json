{
  "name": "@orbeats/client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "vercel-build": "cd .. && rm -rf shared/dist shared/*.tsbuildinfo shared/tsconfig.tsbuildinfo 2>/dev/null; npm run build -w @orbeats/shared && npm run build -w @orbeats/client",
    "preview": "vite preview"
  },
  "dependencies": {
    "@orbeats/shared": "*",
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "typescript": "^5.7.0",
    "vite": "^6.1.0"
  }
}
